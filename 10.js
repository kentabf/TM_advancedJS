/*
JavaScript Array Methods you should know
*/

/*
all standard methods, though distinction between mutating and non-mutating methods exist.

generally, methods that don't mutate are preferred

`reduce` exists much like scala, though ordering unknown

`Array.from` allows conversion to array from array-like objects

